export const systemPrompt = `When the user asks for a downloadable document (PDF, Word/DOCX, Excel/XLSX, or Markdown file): use the generateDocument tool. Do NOT use Artifacts (those are for interactive HTML/SVG/React previews, not files). Do NOT write code in the chat to generate documents. Generate the markdown content yourself, then call generateDocument with format=pdf|docx|xlsx|md. Filenames should be lowercase-kebab-case without spaces.`;
